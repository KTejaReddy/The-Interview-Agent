# Root .env — consumed by docker-compose.
# Copy to .env, then fill in LLM_API_KEY. Backend defaults live in
# backend/.env.example too.

LLM_PROVIDER=openai_compatible
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini
LLM_API_KEY=
LLM_MOCK_MODE=false
LLM_JSON_MODE=true
LOG_LEVEL=INFO
